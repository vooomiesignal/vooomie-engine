# VOOOMIE Trends Viral Engine — Python Dependencies
# Install with: pip install -r requirements.txt

anthropic>=0.28.0          # Claude AI content rewriter
feedparser>=6.0.10         # RSS feed parsing for all sources
requests>=2.31.0           # HTTP calls (WordPress API, Telegram, Unsplash)
python-dotenv>=1.0.0       # Load .env config file
